# 🖥 Week 01. Як працює Інтернет? HTTP, клієнт-сервер, браузер

---

## 🧑‍🏫 Лекція (1,5 години)

### Вступ (10 хв)

1. **Прочитайте уважно:** сьогодні ми починаємо вивчати веб-програмування.

   * Мета курсу: навчитися створювати сайти з нуля.
   * На цьому тижні ви зрозумієте, як працює Інтернет і браузер.

2. **Чому це важливо:**

   * Ви не лише писатимете код, а й розумітимете, що відбувається, коли хтось відкриває ваш сайт.
   * Це допоможе швидше знаходити помилки і краще працювати в команді.

3. **Приклади з життя:**

   * Google: ви вводите слово → браузер надсилає запит → сервер відповідає сторінкою.
   * YouTube: ви клікаєте на відео → сервер відправляє відео у браузер.
   * Rozetka: ви купуєте товар → браузер надсилає дані про замовлення на сервер → сервер відповідає підтвердженням.

Порівняйте браузер з офіціантом, сервер з кухнею, а Інтернет із дорогою між ними.

---

### 1. Що таке Інтернет? (15 хв)

1. **Визначення:** Інтернет — це світова мережа, яка об’єднує мільйони пристроїв.

2. **Основні елементи:**

   * Клієнт (ваш комп’ютер, смартфон).
   * Сервер (комп’ютер, де “живе” сайт).
   * DNS (служба, яка перетворює ім’я сайту на його IP-адресу).

3. **Приклад:**

   * Ви вводите в браузері `google.com`.
   * DNS знаходить його адресу: наприклад, `142.250.185.14`.
   * Браузер відправляє запит на цю адресу.
   * Сервер відповідає і віддає сторінку.

📌 **Міні-завдання:** намалюйте просту схему на аркуші:

```
Ви → DNS → Сервер → Відповідь → Ви
```

---

### 2. Клієнт-серверна модель (15 хв)

1. **Клієнт** – пристрій, який просить дані.
2. **Сервер** – комп’ютер, який дає дані.

📌 **Приклади:**

* Gmail → клієнт: браузер, сервер: поштовий сервер Google.
* Instagram → клієнт: додаток, сервер: база з фото.
* Netflix → клієнт: телевізор/браузер, сервер: сервери з відео.

📌 **Міні-завдання:**
Скажіть, хто є клієнтом, а хто сервером у ситуації:

* Ви на телефоні відправляєте повідомлення у Telegram.
* Ви на телевізорі дивитеся відео на YouTube.
* Ви на компьютері читаєте статтю на Wikipedia.

---

### 3. HTTP-протокол (20 хв)

1. **Що це таке:** HTTP — мова, якою клієнт і сервер обмінюються повідомленнями.

2. **Приклад HTTP-запиту:**

```http
GET /index.html HTTP/1.1
Host: example.com
```

(Браузер каже: “дай мені файл index.html”)

3. **Приклад HTTP-відповіді:**

```http
HTTP/1.1 200 OK
Content-Type: text/html
```

(Сервер відповідає: “Ось твоя сторінка”)

4. **Важливі методи:**

* `GET` — отримати дані (сторінку).
* `POST` — відправити дані (форма).
* `PUT/DELETE` — оновити чи видалити.

5. **Коди стану:**

* `200` — все добре.
* `404` — сторінка не знайдена.
* `500` — помилка сервера.

📌 **Міні-демонстрація (для самостійної роботи):**

1. Відкрийте будь-який сайт.
2. Натисніть правою кнопкою → “Перевірити” (або `F12`).
3. Відкрийте вкладку **Network**.
4. Перезавантажте сторінку і подивіться список запитів.

---

### 4. Як працює браузер (20 хв)

1. Браузер отримує HTML.
2. Будує **DOM-дерево** (структуру сторінки) DOM - Document Object Model.
3. Завантажує CSS → застосовує стилі.
4. Завантажує JS → додає поведінку.
5. Відображає результат.

📌 **Аналогія:**

* HTML = скелет.
* CSS = одяг.
* JS = поведінка.

📌 **Міні-завдання:**
Відкрийте у Chrome будь-який сайт → натисніть **F12 → Elements** → подивіться, які теги там є.

---

### 5. Узагальнення (10 хв)

1. Інтернет = клієнти + сервери + мережа.
2. HTTP = мова спілкування.
3. Браузер = інтерфейс, який показує результат.
4. Цикл:

   ```
   Ввів адресу → DNS → HTTP-запит → Сервер → Відповідь → Сторінка
   ```

📌 **Питання для перевірки:**

1. Що таке DNS?
2. Чим клієнт відрізняється від сервера?
3. Який код означає “сторінку не знайдено”?
4. Що робить браузер з HTML-кодом?

---

## 🛠 Практика (1,5 години)

### 1. Створення першої сторінки (15 хв)

1. Встановіть редактор коду (наприклад, **VS Code**).

   * Завантажте з [https://code.visualstudio.com/](https://code.visualstudio.com/).
   * Встановіть як звичайну програму.

2. Створіть папку `week01`.

3. У ній — файл `index.html`.

4. Скопіюйте цей код:

```html
<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8">
  <title>Моя перша сторінка</title>
</head>
<body>
  <h1>Привіт, світ!</h1>
  <p>Це моя перша HTML-сторінка 🚀</p>
</body>
</html>
```

5. Збережіть файл.
6. Відкрийте його подвійним кліком у браузері.

---

### 2. Дослідження тегів (30 хв)

1. Відкрийте свій `index.html`.
2. Додайте у середину тега body кілька нових рядків:

```html
<h2>Про мене</h2>
<p>Мене звати Іван. Я навчаюся веб-програмуванню.</p>

<h2>Мої хобі</h2>
<ul>
  <li>Читання</li>
  <li>Подорожі</li>
  <li>Футбол</li>
</ul>

<h2>Контакти</h2>
<a href="https://facebook.com">Мій Facebook</a>
```

3. Збережіть файл → перезавантажте сторінку у браузері.
4. Додайте зображення:

```html
<img src="https://via.placeholder.com/150" alt="Тестове фото">
```

📌 **Результат:** у вас повинна бути проста візитка.

---

### 3. Робота з DevTools (15 хв)

1. Відкрийте свою сторінку в браузері.
2. Натисніть `F12` → вкладка **Elements**.
3. Знайдіть ваш заголовок `<h1>`.
4. Подвійним кліком змініть текст прямо у браузері.

📌 **Зверніть увагу:** після оновлення сторінки зміни зникнуть — тому треба редагувати у файлі.

---

### 4. Завершальне завдання (30 хв)

1. Додайте таблицю у свій файл:

```html
<h2>Мій розклад</h2>
<table border="1">
  <tr><th>День</th><th>Заняття</th></tr>
  <tr><td>Понеділок</td><td>Програмування</td></tr>
  <tr><td>Середа</td><td>Англійська</td></tr>
</table>
```

2. Додайте форму:

```html
<h2>Напишіть мені</h2>
<form>
  <label>Ім’я: <input type="text"></label><br>
  <label>Email: <input type="email"></label><br>
  <input type="submit" value="Надіслати">
</form>
```

3. Збережіть → відкрийте у браузері.

📌 **Результат:** у вас має бути сторінка з:

* заголовком,
* текстом,
* списком,
* фото,
* таблицею,
* формою.

---

## 🏠 Домашнє завдання

Створіть особисту сторінку:

* Ім’я, фото.
* Короткий опис (2–3 абзаци).
* Список улюблених книг/фільмів.
* Посилання на соцмережі.
* Використайте мінімум **7 різних тегів** (h1, p, a, img, ul/li, table, form).

Збережіть файл як `index.html` і відкрийте у браузері.

---

## 📎 Матеріали

* [Як працює Інтернет? (MDN)](https://developer.mozilla.org/uk/docs/Learn/Common_questions/How_does_the_Internet_work)
* [Основи HTML (MDN)](https://developer.mozilla.org/uk/docs/Learn/Getting_started_with_the_web/HTML_basics)
* [reqbin.com](https://reqbin.com/) — сервіс для тестування HTTP-запитів

* [Як працює інтернет: комп’ютери та мережа](https://youtu.be/wqk9ckofM28?si=83ZlbM1YaeHUwXNz)
* [Що таке HTTP та HTTPS. Як працює і в чому різниця](https://youtu.be/6RBx2lkA4us?si=5DyVySfxs4cQ3hOp)
* [Що таке домен і IP-адреса сайту](https://youtu.be/ACYyBPoi4Go?si=bFLyXS_PA_XWVC3T)