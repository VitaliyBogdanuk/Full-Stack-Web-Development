# 🖥 Week 07: JS основи: змінні, типи, оператори

---

## 🧑‍🏫 Лекція (1,5 години)

### Вступ (10 хв)

1. До цього ми працювали тільки з HTML і CSS — вони відповідають за **структуру і вигляд** сторінки.
2. Але сайти без логіки — “статичні картинки”.
3. Щоб сайт реагував на дії користувача (кнопки, підрахунки, анімації) потрібен **JavaScript**.
4. Сьогодні розберемо:

   * Як створювати **змінні**.
   * Які бувають **типи даних**.
   * Як працюють **оператори**.

---

### 1. Підключення JS (10 хв)

1. Додати JS у `<script>` в HTML:

```html
<script>
  console.log("Привіт, JS!");
</script>
```

2. Рекомендований спосіб — у зовнішньому файлі:

```html
<script src="script.js"></script>
```

У `script.js`:

```js
console.log("Привіт, JS!");
```

📌 Відкрити у браузері → `F12` → вкладка **Console** → побачити повідомлення.

---

### 2. Змінні (20 хв)

1. Що таке змінна? → “коробка”, де зберігається значення.
2. Способи оголошення:

```js
let name = "Оля";     // можна змінювати
const pi = 3.14;      // не можна змінювати
var old = "старий";   // майже не використовуємо
```

3. Правила іменування:

   * тільки латиниця, цифри, `_`, `$`;
   * не починати з цифри;
   * зрозумілі назви (camelCase).

📌 Міні-вправа: створіть змінні `age`, `city`, `isStudent` і виведіть у консоль.

---

### 3. Типи даних (25 хв)

**Примітивні типи:**

* `string` — рядок (`"Привіт"`).
* `number` — число (`42`, `3.14`).
* `boolean` — логічний (`true`, `false`).
* `null` — “нічого”.
* `undefined` — не визначено.

**Об’єкти (коротко):**

* `object` — колекція даних.
* `array` — список.

📌 Приклади:

```js
let name = "Іван";       // string
let age = 20;            // number
let isStudent = true;    // boolean
let car = null;          // null
let house;               // undefined
```

📝 Міні-завдання: створіть по одному прикладу кожного типу.

---

### 4. Оператори (25 хв)

**Арифметичні:**

```js
let a = 10, b = 5;
console.log(a + b); // 15
console.log(a - b); // 5
console.log(a * b); // 50
console.log(a / b); // 2
console.log(a % b); // 0 (залишок)
```

**Порівняння:**

```js
console.log(5 > 3);   // true
console.log(5 == "5"); // true (нестроге)
console.log(5 === "5"); // false (строге)
```

**Логічні:**

```js
console.log(true && false); // false
console.log(true || false); // true
console.log(!true); // false
```

📌 Міні-завдання:

* Створіть дві змінні `x=8`, `y=3`.
* Виведіть у консоль результати `+`, `-`, `*`, `/`, `%`.
* Перевірте, чи `x` більше за `y`.

---

### Узагальнення (10 хв)

1. JS дозволяє додавати логіку.
2. Змінні = коробки для значень.
3. Типи даних = “форма” значення.
4. Оператори = інструменти для обчислень.

📌 Контрольні питання:

1. Яка різниця між `let` і `const`?
2. Чим відрізняється `==` від `===`?
3. Який тип даних у змінної `let x;`?

---

## 🛠 Практика (1,5 години)

### Завдання: калькулятор у консолі

---

📌 **Крок 1. Створіть файли (10 хв)**

* Папка `week07`.
* `index.html` з підключеним `script.js`.

```html
<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8">
  <title>JS калькулятор</title>
</head>
<body>
  <h1>JS Калькулятор (у консолі)</h1>
  <script src="script.js"></script>
</body>
</html>
```

---

📌 **Крок 2. Змінні (15 хв)**
У `script.js`:

```js
let a = 10;
let b = 5;

console.log("a =", a, "b =", b);
```

---

📌 **Крок 3. Арифметика (25 хв)**

```js
console.log("Сума:", a + b);
console.log("Різниця:", a - b);
console.log("Добуток:", a * b);
console.log("Ділення:", a / b);
console.log("Залишок:", a % b);
```

---

📌 **Крок 4. Порівняння (20 хв)**

```js
console.log("a > b ?", a > b);
console.log("a < b ?", a < b);
console.log("a == b ?", a == b);
console.log("a === b ?", a === b);
```

---

📌 **Крок 5. Логіка (20 хв)**

```js
let isAdult = true;
let hasMoney = false;

console.log("Чи можу я піти в кіно?");
console.log(isAdult && hasMoney); // true тільки якщо обидва умови виконані
```

---

📌 **Крок 6. Фінальна перевірка (10 хв)**

* Відкрити сторінку → `F12` → Console.
* Чи працює калькулятор?
* Чи видно результати всіх операцій?

---

## 🏠 Домашнє завдання

**Тема: розрахунок бюджету.**

Створіть програму `budget.js`, яка:

1. Має змінні:

   * `income` (дохід на місяць).
   * `rent` (оренда).
   * `food` (їжа).
   * `transport` (транспорт).
   * `entertainment` (розваги).
2. Обчислює `totalExpenses = rent + food + transport + entertainment`.
3. Обчислює `savings = income - totalExpenses`.
4. Виводить у консоль:

   * загальні витрати,
   * залишок грошей,
   * повідомлення:

     * якщо `savings > 0` → “У вас залишилися гроші”.
     * якщо `savings == 0` → “Ви витратили все”.
     * якщо `savings < 0` → “Ви в мінусі!”.

📌 Очікуваний результат у консолі:

```
Загальні витрати: 12000
Залишок: -2000
Ви в мінусі!
```

---

## 📎 Матеріали/Нотатки

* [MDN: Вступ до JavaScript](https://developer.mozilla.org/uk/docs/Learn/JavaScript/First_steps)
* [MDN: Змінні](https://developer.mozilla.org/uk/docs/Learn/JavaScript/First_steps/Variables)
* [MDN: Типи даних](https://developer.mozilla.org/uk/docs/Web/JavaScript/Data_structures)
* [MDN: Оператори](https://developer.mozilla.org/uk/docs/Web/JavaScript/Guide/Expressions_and_Operators)